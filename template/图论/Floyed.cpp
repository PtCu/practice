#include <bits/stdc++.h>
using namespace std;
const int maxn = 500;
vector<int>G[maxn + 5];
int n, m; //n个点，m条边
int main()
{
    cin >> n >> m;
    int x, y, l;
    for (size_t i = 0; i < m; ++i)
    {
        cin >> x >> y >> l;
    }
}