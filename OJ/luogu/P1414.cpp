#include <bits/stdc++.h>
using namespace std;
const int maxn = 1e4 + 4;
int source[maxn];
int ans[maxn];
int main()
{
    int n;
    cin>>n;
    int gcd = 1;
    for (int i = 1; i <= n; ++i)
    {
        cin >> source[i];
    }

}