#include<bits/stdc++.h>
using namespace std;
const int maxn = 1e4;
vector<int> G[maxn];
int main(){
    int n, m;
    cin >> n >> m;
    int s,x;
    for (size_t i = 0; i < m;++i){
        cin >> s;
        for (size_t j = 0; j < s;++j){
            
        }
    }
}