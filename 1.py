import numpy as np
# S = np.array([-1, 1, -3, 1, -1, -3, 1, 1])
# A = np.array([-1, -1, -1, 1, 1, -1, 1, 1])
# B = np.array([-1, -1, 1, -1, 1, 1, 1, -1])
# C = np.array([-1, 1, -1, 1, 1, 1, -1, -1])
# D = np.array([-1, 1, -1, -1, -1, -1, 1, -1])
# a = np.dot(S, A)/8
# b = np.dot(S, B)/8
# c = np.dot(S, C)/8
# d = np.dot(S, D)/8
# print(a, b, c, d)
print(112 & 240)
